const getJobs = require("./getJobs");
const searchJob = require("./searchJob");
const scrapeJob = require("./scrapeJobs");
module.exports = {
    getJobs: getJobs,
    searchJob: searchJob,
    scrapeJob: scrapeJob
};
